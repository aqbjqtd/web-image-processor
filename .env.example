# Web图像处理应用环境变量配置文件
# 复制此文件为 .env 并根据实际情况修改配置

# === 应用配置 ===
# 应用端口
APP_PORT=8080

# 时区设置
TZ=Asia/Shanghai

# 构建信息（自动生成，通常不需要修改）
BUILD_DATE=
VCS_REF=

# === Nginx 配置 ===
# Nginx 工作进程数（auto 表示自动检测 CPU 核心数）
NGINX_WORKER_PROCESSES=auto

# Nginx 连接数
NGINX_WORKER_CONNECTIONS=2048

# === 数据存储路径 ===
# 数据根目录（相对于项目根目录）
DATA_DIR=./data

# === Redis 配置（可选） ===
# Redis 端口
REDIS_PORT=6379

# Redis 密码（留空表示无密码）
REDIS_PASSWORD=

# === Traefik 配置（生产环境） ===
# 域名（生产环境使用）
DOMAIN=localhost

# Traefik 仪表板端口
TRAEFIK_DASHBOARD_PORT=8081

# Traefik 安全模式（生产环境设为 false）
TRAEFIK_INSECURE=true

# Let's Encrypt 邮箱（HTTPS 证书申请）
ACME_EMAIL=admin@example.com

# Traefik 日志级别（DEBUG, INFO, WARN, ERROR）
TRAEFIK_LOG_LEVEL=INFO

# === 监控配置 ===
# Prometheus 端口
PROMETHEUS_PORT=9090

# Grafana 端口
GRAFANA_PORT=3000

# Grafana 管理员密码
GRAFANA_PASSWORD=admin123

# === 部署配置说明 ===
# 
# 基础部署：
# docker-compose up -d
# 
# 包含缓存服务：
# docker-compose --profile cache up -d
# 
# 包含 Traefik 反向代理：
# docker-compose --profile traefik up -d
# 
# 包含监控服务：
# docker-compose --profile monitoring up -d
# 
# 完整部署（所有服务）：
# docker-compose --profile traefik --profile monitoring --profile cache up -d
# 
# === 注意事项 ===
# 1. 生产环境请修改默认密码
# 2. 生产环境请设置正确的域名和邮箱
# 3. 确保防火墙正确配置端口
# 4. 定期备份数据目录
